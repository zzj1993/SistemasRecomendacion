h2Config:
  driverClass: org.h2.Driver
  user: sa
  password: sa
  url: jdbc:h2:tcp://localhost//Users/Pisco/test
  
dataConfig:
  dir: /Users/Pisco/git/SistemasRecomendacion/recomendadorPeliculas/target/
  movies: movies.dat
  ratings: ratings.dat
  users: users.dat
  load: false

logging:
  level: INFO
  loggers:
    io.dropwizard: INFO
  appenders:
    - type: console
      threshold: ALL
      timeZone: UTC
      target: stdout
      logFormat: # TODO