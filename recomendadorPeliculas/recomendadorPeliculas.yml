h2Config:
  driverClass: org.h2.Driver
  user: sa
  password: sa
  url: jdbc:h2:./target/db
  
dataConfig:
  dir: ./target/
  movies: movies.dat
  rattings: rattings.dat
  users: users.dat 

logging:
  level: INFO
  loggers:
    io.dropwizard: INFO
  appenders:
    - type: console
      threshold: ALL
      timeZone: UTC
      target: stdout
      logFormat: # TODO